import type { Order } from "@prisma/client"


const OrderCards = ({ Order }: { Order: Order}) => {
  // to do later because we still need to handle orders no orders data yet
  
  return (
    <div>Order</div>
  )
}

export default OrderCards